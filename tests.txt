Backend tests failed:
- 4 tests failed
- Jest did not exit cleanly, suggesting open handles. Need to investigate with `--detectOpenHandles`.

Update: app.test.ts passes, but still causes Jest to hang with open handles.