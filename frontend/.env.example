# Frontend environment example (no secrets)
NODE_ENV=development
PORT=3000

# API base URL for the frontend to reach backend
VITE_API_URL=http://localhost:8001/api

# Stripe publishable key and price ids used on client
VITE_STRIPE_PUBLISHABLE_KEY=
VITE_STRIPE_MONTHLY_PRICE_ID=
VITE_STRIPE_ANNUAL_PRICE_ID=
